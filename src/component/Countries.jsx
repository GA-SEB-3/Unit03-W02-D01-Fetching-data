import React from 'react'

function Countries() {
    // Exercise 1:
    // get all the countries in the rest countries API: https://restcountries.com/v3.1/name/all
    // store them in a state
    // map through all the values and show just the name of the country
    // BONUS: show the capital, population, and flag
  return (
    <div>
      
    </div>
  )
}

export default Countries
